import 'package:shadcn_flutter/shadcn_flutter.dart' as shad;

// Window is an upstream configuration object; WindowWidget is the directly
// rendered primitive and WindowNavigator manages multiple configurations.
typedef AppWindow = shad.Window;
typedef AppWindowWidget = shad.WindowWidget;
typedef AppWindowNavigator = shad.WindowNavigator;
typedef AppWindowController = shad.WindowController;
typedef AppWindowState = shad.WindowState;
typedef AppWindowSnapStrategy = shad.WindowSnapStrategy;
typedef AppWindowActions = shad.WindowActions;
typedef AppWindowViewport = shad.WindowViewport;
