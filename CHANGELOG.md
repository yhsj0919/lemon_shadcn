## 0.0.1

* Convert the generated plugin skeleton into a cross-platform component package.
* Add `shadcn_flutter` and Lemon light/dark theme entry points.
* Add a minimal component gallery and tests.
